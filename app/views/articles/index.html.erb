<h1>Recipes List</h1>
<%= link_to 'New Recipe', new_article_path %>
<br />
<br />
<table>
  <tr>
    <th>Meal</th>
    <th>Ingredients</th>
    <th>Image</th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td><%= image_tag article.image.url(:thumb) %></td>
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Delete',
                             article_path(article),
                             method: :delete,
                             data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>
<br />
<br />
<%= link_to 'Welcome Page', controller: 'welcome' %><br>